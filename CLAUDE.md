# Forensic Intelligence Workshop — Claude Demo

## Workshop Overview

This interactive workshop demonstrates how Claude can support forensic and investigative workflows across three stages:

1. **Data Analysis** — linking structured and unstructured data sources
2. **Corporate Intelligence** — retrieving and synthesising information from the web
3. **Dashboard** — vibe-coding a live investigation dashboard

---

## The Investigation Narrative

> A financial crimes unit has received a whistleblower tip about irregular payments at an energy company. They have three data sources: a transaction export, a corporate email archive, and a public offshore leaks database. None of the sources alone is conclusive. The task is to connect them.

Participants will discover (in order):

1. A cluster of transfers just below the $10,000 reporting threshold going to an entity called *Chewco Investments L.P.*
2. Emails in which a senior executive discusses "partnership arrangements" with offshore vehicles around the same dates
3. Three large transfers that leave Enron Corp, pass through *LJM Cayman* and *JEDI Partners*, and return — debt disguised as equity
4. A $26.8M payment to *Southampton Place L.P.* three days before the company's collapse
5. All of the above entities appearing in the ICIJ Offshore Leaks database

This is not fiction — these are the real Enron SPEs, real executive names, and real forensic patterns from the scandal.

---

## Datasets

### Structured — Financial Transactions (generated)

Generated by `scripts/generate_transactions.py`. Seeded with real Enron entity names.

| File | Description |
|------|-------------|
| `data/structured/transactions.csv` | 2,000 transactions, 1999–2001 (workshop version) |
| `data/structured/transactions_facilitator.csv` | Same data with `flag_type` column (facilitator only) |
| `data/structured/accounts.json` | 27 accounts: Enron Corp, executives, shells, legitimate counterparties |
| `data/structured/extracted_entities.csv` | Entities extracted from Enron emails by `extract_enron_entities.py` |

**Embedded forensic patterns:**

| Pattern | Description | Count |
|---------|-------------|-------|
| `structuring` | Transfers to Chewco of $9,100–$9,850 (just below $10k threshold) | 14 |
| `round_trip_leg1/2/3` | Enron → LJM Cayman → JEDI → Enron (3 full cycles) | 9 |
| `shell_payment` | Consulting/advisory fees to Cayman/BVI shells | 45 |
| `high_value_shell` | $26.8M to Southampton Place on 2001-09-28 | 1 |
| `legitimate` | Normal energy business transactions | ~1,931 |

### Unstructured — Enron Email Corpus

Download separately (not included in this repo — ~1.7GB):

```
Search: "Enron email dataset Kaggle" or "CMU Enron corpus"
Expected structure: maildir/<person>/<folder>/<email_number>  (no extension)
```

Run `scripts/extract_enron_entities.py` to produce `data/structured/extracted_entities.csv`.

### Reference — ICIJ Offshore Leaks

Download separately from `offshoreleaks.icij.org/pages/database`. After downloading,
run `scripts/build_icij_bridge.py` to produce `data/reference/icij_bridge.csv`.

---

## Setup

### Prerequisites

- Python 3.11+
- `ANTHROPIC_API_KEY` set in environment

### Install dependencies

```bash
pip install -r requirements.txt
```

### Generate the transaction dataset

```bash
python scripts/generate_transactions.py
# Options:
#   --seed 42               reproducible output (default)
#   --num-transactions 2000 total transaction count (default)
```

### Extract entities from Enron emails (requires downloaded corpus)

```bash
python scripts/extract_enron_entities.py --enron-dir /path/to/maildir
```

### Build the ICIJ bridge (requires downloaded ICIJ bulk data)

```bash
python scripts/build_icij_bridge.py --icij-nodes /path/to/nodes-entities.csv
```

---

## Stage 1: Structured & Unstructured Data Analysis

### Goal
Show how Claude can extract, cross-reference, and surface insights from mixed data formats — the kind of fragmented evidence typical in forensic investigations.

### What to demonstrate

1. **Entity extraction from emails** — give Claude a batch of Enron emails and ask it to pull out company names, people, dollar amounts, and dates
2. **Cross-reference against transactions** — ask Claude to find any counterparty names from the emails that also appear in `transactions.csv`
3. **Flag anomalies** — ask Claude to identify unusual patterns in the transaction data (frequency, amount clustering, offshore counterparties)
4. **Build an entity timeline** — for a single entity (e.g. LJM Cayman), show all emails mentioning it alongside all transactions involving it, ordered chronologically

### Sample prompts

```
Here are 20 Enron emails [paste text]. Extract every company name, person name,
and dollar amount mentioned. Return them as a structured table.
```

```
Here is a CSV of financial transactions [attach transactions.csv] and a list of
entities extracted from corporate emails [attach extracted_entities.csv].
Identify any transaction counterparties that match entities from the emails.
Flag transactions above $1M to offshore jurisdictions (KY or VG).
```

```
In transactions.csv, are there any counterparties that receive multiple payments
in a short period, each just below $10,000? List them with the dates and amounts.
```

---

## Stage 2: Corporate Intelligence (Web Research)

### Goal
Enrich the investigation with open-source intelligence — public corporate, financial, and reputational data retrieved live from the web.

### What to demonstrate

1. **Company lookup** — starting from a shell entity name found in Stage 1, look it up in public registries
2. **Director chain** — from a director name, find all associated companies and their statuses
3. **Sanctions and PEP check** — cross-reference names against OpenSanctions
4. **Adverse media** — search for news linking entities to legal actions or regulatory findings

### Sample prompts

```
Research "LJM Cayman L.P." — find its registration details, known directors,
any litigation, and any mentions in regulatory or enforcement actions.
Summarise with source links.
```

```
Starting from the name "Andrew Fastow", identify all companies he was associated
with, their jurisdictions, and any public enforcement actions or convictions.
```

```
Cross-reference these entity names against the OpenSanctions database and any
known PEP lists. Flag any matches. [paste entity list]
```

### Important notes
- Always cite sources; distinguish verified public records from news
- Note data freshness — registry data can lag weeks or months
- OSINT is a starting point, not a conclusion; flag items needing formal verification

---

## Stage 3: Vibe-Coding the Investigation Dashboard

### Goal
Live-code a simple web dashboard that ties stages 1 and 2 together — an interface investigators could actually use.

### Features to build iteratively

1. **Entity search** — search by name, account ID, or transaction ID
2. **Timeline view** — plot transactions on a chronological axis, with email events overlaid
3. **Network graph** — visualise entity relationships (people ↔ companies ↔ transactions)
4. **Flag & annotate** — mark entities as suspicious / verified / needs review
5. **Export** — generate a case summary (PDF or structured JSON)

### Suggested stack

| Layer | Choice |
|-------|--------|
| Frontend | React + Vite or plain HTML/JS |
| Graph | Cytoscape.js or D3.js |
| Backend | FastAPI (Python) |
| Data store | SQLite (swap out later) |
| AI integration | Anthropic Python SDK with streaming |

### Vibe-coding starter prompts

```
Build a single-page app with a search box. When I type a company name,
it calls a local FastAPI endpoint that returns related accounts and transactions
as JSON, and renders them as nodes in a Cytoscape.js graph.
```

```
Add a right-hand sidebar that shows all transactions for a selected node,
sorted by date, with amounts colour-coded: green for inbound, red for outbound.
```

```
Add a timeline panel below the graph. For each selected entity, plot its
transactions as points on a horizontal time axis from 1999 to 2002.
Mark the dates of known Enron events (e.g. 2001-10-16 earnings restatement,
2001-12-02 bankruptcy filing) as vertical reference lines.
```

---

## Workshop Session Plan

| Time | Activity |
|------|----------|
| 0–10 min | Intro: the Enron scenario, what we're trying to find |
| 10–15 min | Dataset walkthrough: transactions, emails, ICIJ |
| 15–40 min | Stage 1: live data analysis with Claude |
| 40–60 min | Stage 2: corporate intelligence and OSINT |
| 60–90 min | Stage 3: vibe-coding the dashboard |
| 90–100 min | Debrief: what Claude found, what it missed, Q&A |

---

## File Structure

```
.
├── CLAUDE.md
├── requirements.txt
├── scripts/
│   ├── extract_enron_entities.py   # scan Enron corpus → extracted_entities.csv
│   ├── generate_transactions.py    # generate synthetic transaction dataset
│   └── build_icij_bridge.py        # filter ICIJ data → icij_bridge.csv
└── data/
    ├── structured/
    │   ├── transactions.csv              # generated (workshop version)
    │   ├── transactions_facilitator.csv  # generated (with flag_type — keep hidden)
    │   ├── accounts.json                 # generated
    │   └── extracted_entities.csv        # generated from Enron corpus
    ├── unstructured/
    │   ├── emails/                       # Enron .txt email files (download separately)
    │   └── invoices/                     # optional: scanned PDF invoices
    └── reference/
        └── icij_bridge.csv               # generated from ICIJ bulk download
```

---

## Key Principles for Forensic AI Use

- **Claude as analyst, human as investigator** — Claude surfaces patterns; a human makes the call
- **Auditability** — log every prompt and response; the investigation record must be reproducible
- **No hallucination tolerance** — treat any factual claim about real entities as a hypothesis to verify, not a finding
- **Data minimisation** — only send Claude the data relevant to the query
- **Legal and ethical boundaries** — OSINT only; no scraping of access-controlled systems, no personal data beyond what is legally permitted in your jurisdiction
